let chapatis = [1, 2, 3];
let reqNum = 3;

if (reqNum < chapatis.length || reqNum === chapatis.length) {
  console.log('Enough chapatis available');
} else {
  console.log('Sorry we out of stock!');
}
